<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Contact - Streak Sync</title>
  <style>
    :root {
      --bg-color: #f7f7f7;
      --text-color: #111;
      --container-bg: #fff;
      --button-bg: #9B177E;
      --button-hover: #9B177E;
      --input-border: #ccc;
    }

    [data-theme="dark"] {
      --bg-color: #121212;
      --text-color: #e0e0e0;
      --container-bg: #1e1e1e;
      --button-bg: #9B177E;
      --button-hover: #9B177E;
      --input-border: #555;
    }

    body {
      margin: 0;
      padding: 0;
      background-color: var(--bg-color);
      color: var(--text-color);
      transition: background 0.3s, color 0.3s;
    }

    a[role="button"] {
      display: inline-block;
      margin: 10px;
      text-decoration: none;
      background: var(--button-bg);
      color: white;
      padding: 10px 20px;
      border-radius: 8px;
      transition: background 0.3s;
    }

    a[role="button"]:hover {
      background: var(--button-hover);
    }

    .topnav {
      background-color: #333333;
      overflow: hidden;
    }

    .topnav a {
      float: left;
      color: #f2f2f2;
      text-align: center;
      padding: 14px 16px;
      text-decoration: none;
      font-size: 17px;
    }

    .topnav a:hover {
      background-color: #ddd;
      color: black;
    }

    .topnav a.active {
      background-color: #9B177E;
      color: white;
    }

    .container {
      max-width: 900px;
      margin: 100px auto 50px;
      background-color: var(--container-bg);
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }

    .contact-box {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    .contact-card {
      background-color: var(--bg-color);
      border: 1px solid var(--input-border);
      border-radius: 8px;
      padding: 20px;
    }

    .contact-card h3 {
      margin: 0 0 10px;
      color: var(--text-color);
    }

    .contact-card a {
      display: block;
      margin: 5px 0;
      color: #9B177E;
      text-decoration: none;
    }

    .contact-card a:hover {
      text-decoration: underline;
    }

    #dark-toggle {
      height: 70px;
      border-radius: 35px;
      position: fixed;
      bottom: 50px;
      right: 30px;
      cursor: pointer;
      z-index: 1000;
    }

    .footer {
      margin-top: 40px;
      padding: 15px 0;
      background-color: var(--container-bg);
      color: var(--text-color);
      text-align: center;
      box-shadow: 0 -1px 5px rgba(0,0,0,0.1);
      border-top: 1px solid var(--input-border);
    }

    .footer-content {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }

    .footer-content img {
      height: 40px;
      border-radius: 20px;
      margin-bottom: 5px;
    }

    a, button, p, h1, h2, h3, span {
      font-family: 'Lucida Sans', sans-serif;
    }
  </style>
</head>
<body>
  <div class="topnav"> 
    <img src="/images/logo.png" alt="Logo" style="height: 40px; border-radius: 20px; float: left; padding: 5px 10px;">
    <a href="/app">Home</a>
    <a class="active" href="/contact">Contact</a>
    <a href="/about">About</a>
    <a href="/logout">Log Out</a>
  </div>

  <div class="container">
    <h1>Contact Us</h1>
    <div class="contact-box">
      <div class="contact-card">
        <h3>Tarun Kumar</h3>
        <a href="https://www.linkedin.com/in/tarungsde" target="_blank">LinkedIn</a>
        <a href="https://www.instagram.com/_tzrun_" target="_blank">Instagram</a>
        <a href="mailto:tarung.sde@gmail.com">tarung.sde@gmail.com</a>
      </div>

      <div class="contact-card">
        <h3>Sanjay</h3>
        <a href="https://www.linkedin.com/in/rahul-sinha456" target="_blank">LinkedIn</a>
        <a href="https://www.instagram.com/rahul_sinha" target="_blank">Instagram</a>
        <a href="mailto:rahul@example.com">rahul@example.com</a>
      </div>
    </div>
  </div>

  <img src="/images/torch.png" id="dark-toggle">

  <footer class="footer">
    <div class="footer-content">
      <img src="/images/logo.png" alt="Streak Sync Logo">
      <p>© 2025 Streak Sync. All rights reserved.</p>
    </div>
  </footer>

  <script>
    const toggle = document.getElementById("dark-toggle");
    const body = document.body;

    function applyTheme(theme) {
      body.setAttribute("data-theme", theme);
      localStorage.setItem("theme", theme);
    }

    toggle.addEventListener("click", () => {
      const current = body.getAttribute("data-theme");
      const next = current === "dark" ? "light" : "dark";
      applyTheme(next);
    });

    const savedTheme = localStorage.getItem("theme") || "light";
    applyTheme(savedTheme);
  </script>
</body>
</html>
