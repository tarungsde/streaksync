<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>About - Streak Sync</title>
  <style>
    :root {
      --bg-color: #f7f7f7;
      --text-color: #111;
      --container-bg: #fff;
      --button-bg: #9B177E;
      --button-hover: #9B177E;
      --input-border: #ccc;
    }

    [data-theme="dark"] {
      --bg-color: #121212;
      --text-color: #e0e0e0;
      --container-bg: #1e1e1e;
      --button-bg: #9B177E;
      --button-hover: #9B177E;
      --input-border: #555;
    }

    body {
      margin: 0;
      padding: 0;
      background-color: var(--bg-color);
      color: var(--text-color);
      transition: background 0.3s, color 0.3s;
    }

    a[role="button"] {
      display: inline-block;
      margin: 10px;
      text-decoration: none;
      background: var(--button-bg);
      color: white;
      padding: 10px 20px;
      border-radius: 8px;
      transition: background 0.3s;
    }

    a[role="button"]:hover {
      background: var(--button-hover);
    }

    .app-container {
      width: 90%;
      margin: 20px auto;
      margin-top: 100px;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 20px;
    }

    .left-container, .right-container {
      padding: 20px;
      background: var(--container-bg);
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }

    .ldiv1, .ldiv2, .ldiv3 {
      margin-bottom: 20px;
    }

    .ldiv2 ul {
      list-style: none;
      padding: 0;
    }

    .task-item {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 10px;
    }

    .task-text {
      flex-grow: 1;
      font-size: 16px;
    }

    form {
      display: inline;
    }

    input[type="submit"] {
      margin-left: 5px;
      padding: 10px 15px;
      background-color: #9B177E;
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }

    input[type="submit"]:hover {
      background-color: #000000;
      color: #ffffff;

    }

    input[type="text"] {
      padding: 8px;
      border: 1px solid var(--input-border);
      border-radius: 6px;
      background-color: transparent;
      color: var(--text-color);
    }

    a, button, p, h1, h2, h3, span {
      font-family: 'Lucida Sans', sans-serif;
    }

    .topnav {
      background-color: #333333;
      overflow: hidden;
    }

    .topnav a {
      float: left;
      color: #f2f2f2;
      text-align: center;
      padding: 14px 16px;
      text-decoration: none;
      font-size: 17px;
    }

    .topnav a:hover {
      background-color: #ddd;
      color: black;
    }

    .topnav a.active {
      background-color: #9B177E;
      color: white;
    }

    #dark-toggle {
      height: 70px;
      border-radius: 35px;
      position: fixed;
      bottom: 50px;
      right: 30px;
      cursor: pointer;
      z-index: 1000;
    }
    
    .footer {
      margin-top: 40px;
      padding: 15px 0;
      background-color: var(--container-bg);
      color: var(--text-color);
      text-align: center;
      box-shadow: 0 -1px 5px rgba(0,0,0,0.1);
      border-top: 1px solid var(--input-border);
    }

    .footer-content {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }

    .footer-content img {
      height: 40px;
      border-radius: 20px;
      margin-bottom: 5px;
    }
  </style>
</head>
<body>
  <div class="container">

    <div class="topnav"> 
      <img src="/images/logo.png" alt="Logo" style="height: 40px; border-radius: 20px; float: left; padding: 5px 10px;">
      <a href="/app">Home</a>
      <a href="/contact">Contact</a>
      <a class="active" href="/about">About</a>
      <a href="/logout">Log Out</a>
    </div>

    <div class="about-section" style="max-width: 900px; margin: 100px auto 40px; padding: 40px 30px; background-color: var(--container-bg); border-radius: 20px; box-shadow: 0 4px 20px rgba(0,0,0,0.1);">
    <div style="text-align: center;">
      <img src="/images/logo.png" alt="Streak Sync Logo" style="height: 80px; border-radius: 40px; margin-bottom: 20px;">
      <h1 style="margin-bottom: 10px;">Welcome to <span style="color: var(--button-bg);">Streak Sync</span></h1>
      <p style="font-size: 18px; max-width: 700px; margin: 0 auto;">
        Your personal productivity companion for building consistent habits and getting things done—one streak at a time!
      </p>
    </div>

    <hr style="margin: 30px 0; border: 1px solid var(--input-border);">

    <div style="display: flex; flex-direction: column; gap: 25px;">
      <div style="display: flex; align-items: center; gap: 15px;">
        <span style="font-size: 24px;">✅</span>
        <p style="margin: 0; font-size: 17px;">Quickly add and manage daily tasks.</p>
      </div>
      <div style="display: flex; align-items: center; gap: 15px;">
        <span style="font-size: 24px;">🗑️</span>
        <p style="margin: 0; font-size: 17px;">Delete individual tasks or clear your list in a click.</p>
      </div>
      <div style="display: flex; align-items: center; gap: 15px;">
        <span style="font-size: 24px;">📈</span>
        <p style="margin: 0; font-size: 17px;">Monitor your daily progress with completion percentages.</p>
      </div>
      <div style="display: flex; align-items: center; gap: 15px;">
        <span style="font-size: 24px;">📅</span>
        <p style="margin: 0; font-size: 17px;">Explore your completed tasks using a GitHub-style heatmap.</p>
      </div>
    </div>

    <hr style="margin: 30px 0; border: 1px solid var(--input-border);">

    <div style="text-align: center;">
      <p style="font-size: 16px; max-width: 700px; margin: 0 auto;">
        Whether you're a student, a professional, or just someone looking to build better habits, <strong>Streak Sync</strong> helps you stay focused, motivated, and in control of your daily wins.
      </p>
    </div>
  </div>

     <img src="/images/torch.png" id="dark-toggle"> 
    <footer class="footer">
      <div class="footer-content">
        <img src="/images/logo.png" alt="Streak Sync Logo">
        <p>© 2025 Streak Sync. All rights reserved.</p>
      </div>
    </footer>
    <script>
      const toggle = document.getElementById("dark-toggle");
      const body = document.body;

      function applyTheme(theme) {
        body.setAttribute("data-theme", theme);
        localStorage.setItem("theme", theme);
      }

      toggle.addEventListener("click", () => {
        const current = body.getAttribute("data-theme");
        const next = current === "dark" ? "light" : "dark";
        applyTheme(next);
      });

      // Load saved theme
      const savedTheme = localStorage.getItem("theme") || "light";
      applyTheme(savedTheme);
    </script>
  </div>
</body>
</html>
